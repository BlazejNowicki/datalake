apiVersion: v1
kind: Service
metadata:
  name: {{ include "trino-custom.fullname" . }}
  labels:
    {{- include "trino-custom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trino-custom.selectorLabels" . | nindent 4 }}
