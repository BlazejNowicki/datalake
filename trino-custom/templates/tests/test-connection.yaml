apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trino-custom.fullname" . }}-test-connection"
  labels:
    {{- include "trino-custom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trino-custom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
